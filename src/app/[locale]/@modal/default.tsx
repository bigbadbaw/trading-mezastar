/**
 * Default for the `@modal` parallel slot. Renders nothing, so on a hard load /
 * refresh (no interception) the slot is empty and only the full-page route shows.
 */
export default function ModalDefault() {
  return null;
}
